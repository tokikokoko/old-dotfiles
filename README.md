# What is this?
tokikokoko's .files
Files in the user home directory are symbolically linked.

## nix-pkg
### Install
`curl https://nixos.org/nix/install | sh`

### Channel update
`nix-channel --update`

### Package update
`nix-env -u`
